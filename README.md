#Validate Email C#

Um pequeno projeto em **C#** que valida endereços de e-mail utilizando **expressões regulares (Regex)**.  
A aplicação verifica se o formato do e-mail inserido é válido por exemplo, `user@example.com`.
